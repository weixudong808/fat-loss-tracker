# 测试专用：一键重置（开发者工具，正式用户勿用）

> **用途**：真机测试"新用户裸环境"时，把本地技能与数据清干净。
> **定位**：本文件是纯开发者文档，**不进千问注册包**；将来正式发布时直接删掉本文件即可，技能本体零改动。
> ⚠️ **会删除全部档案、训练计划与记录，不可恢复。** 云端注册包如也需删除，另行让 AI 走平台工具（对它说"把 fat-loss-tracker 从自定义技能里删掉"），文件命令删不掉云端。

在 workspace 目录执行：

```bash
rm -rf skills/fat-loss-tracker fat-loss-tracker fat-loss-tracker-main 减脂数据 减脂追踪数据.xlsx ~/减脂数据 && ls
```

清理范围说明：

| 路径 | 是什么 |
|---|---|
| `skills/fat-loss-tracker/` | 本地技能（标准安装位） |
| `fat-loss-tracker/` | 装错位置的旧克隆（历史遗留） |
| `fat-loss-tracker-main/` | tarball 解压残留（历史遗留） |
| `减脂数据/` | 数据工作簿（档案+全部记录） |
| `减脂追踪数据.xlsx` | workspace 根的附件发送副本 |
| `~/减脂数据/` | 旧版装错位置时落到家目录的数据（历史遗留） |

执行后 `ls` 应只剩你自己的其他文件，即恢复全新环境，可直接重测"新用户首次安装"。
